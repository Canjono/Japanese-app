version: '3'

services:
  frontend:
    build: ./frontend
    volumes:
      - ./frontend/src:/app/src:ro
    ports:
      - "8080:8080"

  backend:
    build: ./backend
    ports:
      - "4000:8080"

  database:
    image: "mariadb"
    ports:
      - "3306:3306"
    volumes:
      - ./database/Vocabulary.sql:/Vocabulary.sql:ro
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_USER=test
      - MYSQL_PASSWORD=test